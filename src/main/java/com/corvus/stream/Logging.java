package com.corvus.stream;

import org.apache.log4j.LogManager;
import org.apache.log4j.Logger;

public interface Logging {
	public static final Logger logger = LogManager.getLogger("FLINK-STREAM");
}
